{
  "name": "mindspace-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "deploy": "git add . && git commit -m \"Update MindSpace\" && git push origin main",
    "deploy:msg": "git add . && git commit && git push origin main"
  }
}
